# Le dice a pytest: "agregá la carpeta actual (backend/) a sys.path antes
# de correr los tests". Así, `from app.main import app` encuentra el
# paquete `app/` sin importar desde qué carpeta invoques `pytest`.
[pytest]
pythonpath = .
